import { useTranslations } from "next-intl";

export default function Home() {
  //const t = useTranslations("InitialPopup");
  return (
    <div className="w-full flex min-h-screen justify-center items-center font-bold">
      main block(скроль вниз)
    </div>
  );
}
